/* ============================================================
   Enlace CRM — configuración
   ------------------------------------------------------------
   Déjalo así y el CRM funciona guardando los datos SOLO en este
   navegador (modo local, sin instalar nada).

   Para trabajar en conjunto con tu socio y ver los cambios en
   tiempo real, crea un proyecto gratuito en https://supabase.com,
   ejecuta el archivo supabase/schema.sql y pega aquí tus datos:

     supabaseUrl:     Project Settings → API → Project URL
     supabaseAnonKey: Project Settings → API → anon public

   La llave "anon" es pública por diseño: la protección real está
   en las políticas RLS del esquema, que exigen sesión iniciada.
   ============================================================ */

window.ENLACE_CONFIG = {
  supabaseUrl: '',
  supabaseAnonKey: '',
};
